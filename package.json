{
  "name": "nexus",
  "private": true,
  "scripts": {
    "dev": "NEXUS_PROJECT_DIR=$PWD pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "node --import tsx packages/server/src/cli.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "node-pty"
    ]
  }
}
